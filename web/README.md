# web/ — LAN dashboard

The HTML dashboard and JSON API will be added in v0.4.

Planned files:

- `index.html` — Chart.js dashboard
- `dashboard.js` — data fetching and rendering
- `chart.min.js` — Chart.js offline bundle
- `api.sh` — CGI script that generates JSON from SQLite
